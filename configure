#!/bin/zsh

START_DIR="$PWD"
cd `dirname $0`

git pull origin master

rsync --exclude ".git" \
      --exclude "configure" \
      --exclude "README.md" \
      -av . ~

source ~/.zshrc

cd $START_DIR
