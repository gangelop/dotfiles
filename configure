#!/bin/zsh

cd `dirname $0`

git pull origin master

rsync --exclude ".git" \
      --exclude "configure" \
      --exclude "README.md" \
      --exclude "LICENSE" \
      -av . ~

source ~/.zshrc

cd $OLDPWD
